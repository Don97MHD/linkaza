# Local previews: leave these defaults. No private credentials needed.
NEXT_PUBLIC_SITE_MODE=preview
INDEXABLE_SITE=false
# Production ONLY, after both domains are connected to this same deployment:
# NEXT_PUBLIC_SITE_MODE=production
# INDEXABLE_SITE=true
